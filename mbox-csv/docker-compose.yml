services:
  web:
    build: .
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - mbox_data:/data
      - mbox_downloads:/downloads
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1

volumes:
  mbox_data:
  mbox_downloads:
